# Use a placeholder for the server list
NTP_SERVERS_PLACEHOLDER

# Standard Chrony settings
driftfile /var/lib/chrony/chrony.drift
logdir /var/log/chrony

# Allow all clients to query this server
allow all

# Continue serving time even if the upstream source is lost
local stratum 10
